<h2>Welcome to Pizza Shop!</h2>
<div class="row">
  <% @products.each do |p| %>
      <div class="col-xs-6 col-md-4 ">
        <div>
        <%= p.title %>
        </div>

        <div>
        <img src = "<%= p.path_to_image %>">
        </div>

        <div>
        opisanie: <%= p.description %>
        </div>

        <div>
          size: <%= p.size %>
        </div>

        <div>
          cennik: <%= p.price %>
        </div>
        <div>
          <button class="btn btn-success btn-sm" onclick="add_to_card(<%= p.id %>)">add to card</button>
        </div>
      </div>
  <% end %>
</div>
<br />
<br />
<br />
<button class="btn btn-primary" onclick="something()"> Click me!</button>
